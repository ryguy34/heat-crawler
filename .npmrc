# pnpm-specific settings for this repo
# Use a stable global store location (optional, good on Windows)
# store-dir=C:/pnpm/store

# Keep install strict, but avoid common peer-deps pain during dev
strict-peer-dependencies=false

# Hoist selected binaries/tools so scripts can find them easily
public-hoist-pattern[]=*eslint*
public-hoist-pattern[]=ts-node
public-hoist-pattern[]=tsx

# Ensure postinstall/preinstall scripts can run when approved
enable-pre-post-scripts=true

# Keep virtual store in default location for determinism
virtual-store-dir=node_modules/.pnpm
